# context-compaction

## Установлено

- `fact:reserve-79-is-headroom` искать: `79%`, `208K`, `check_context_reserve`, `context reserve` — Отказ Кеши при показанных 79% создаёт `ClaudeSession.check_context_reserve`: gate требует `208000 + len(combined.encode("utf-8"))` свободных единиц, поэтому при `totalTokens=790000` prompt 2000 байт проходит, а 2001 байт отклоняется; это predictive headroom, не 95%-порог · `.orchestra/tasks/34/spikes/flow_probe.out`; `claude_session.py:722-834` · 2026-09-03, #34
- `fact:admission-compact-owner` искать: `same batch`, `тот же батч`, `automatic compact`, `_run_batch` — Единственный Kesha-level владелец схемы «автокомпакт и продолжить тот же входящий batch» должен оставаться в `ChatState._run_batch`: тот же coroutine хранит `batch/combined`, сериализует один runtime-specific compact, затем делает ровно один query до дренажа `deferred` · `.orchestra/tasks/34/research.md` § Single-owner transaction; `.orchestra/tasks/34/spikes/flow_probe.out` · 2026-09-03, #34
- `fact:current-compact-helper-drains` искать: `_do_compact`, `deferred`, `first post-compact turn`, `первый ход после компакта` — `ChatState._do_compact` нельзя без изменения вкладывать в admission: `finally` всегда вызывает `_drain_or_idle`, и fake-runtime probe запустил deferred `LATER` до возврата внешнему владельцу `ORIGINAL` · `chat_state.py:1233-1298`; `.orchestra/tasks/34/spikes/flow_probe.out` · 2026-09-03, #34
- `fact:codex-post-compact-unknown` искать: `Codex`, `usage unknown`, `measured_after`, `#24` — После доказанного Codex `contextCompaction` новый размер контекста неизвестен до следующего turn; `compact_context` очищает `_context_tokens/_last_ctx_usage`, а `check_context_reserve` честно допускает один unknown-usage turn, поэтому именно original batch обязан получить первый ход после компакта · `codex_session.py:1124-1135,984-995`; `.orchestra/tasks/24/compact-noop.md`; `tests/test_codex_session.py::test_compact_releases_precompact_high_water_before_next_message` · 2026-09-03, #34
- `fact:codex-native-threshold-ceiling` искать: `Codex 90%`, `95%`, `model_auto_compact_token_limit`, `exact threshold` — Текущий Codex derive-ит native auto-compact threshold как 90% context window и clamp-ит explicit limit максимумом 90%; config schema содержит threshold, но не disable flag, поэтому literal provider-level «только один compactor ровно на 95%» для Codex недоступен · `https://github.com/openai/codex/blob/main/codex-rs/protocol/src/openai_models.rs`; local `codex-cli 0.150.1` schema measurement in `.orchestra/tasks/34/research.md` · 2026-09-03, #34
- `fact:phase1-focused-suite-current-sdk` искать: `238 passed`, `SDK 0.2.152`, `Claude Code 2.1.259`, `focused suite` — Текущие compaction/reserve/runtime oracles проходят на `claude-agent-sdk==0.2.152` с bundled Claude Code 2.1.259: `238 passed, 3 skipped in 16.74s`; эти тесты закрепляют нынешний terminal reserve UX и не доказывают same-batch replay · `.orchestra/tasks/34/spikes/focused-suite.out` · 2026-09-03, #34

## Отвергнуто

- `fact:native-claude-auto-not-guarantee` искать: `native Claude auto-compact`, `DISABLE_AUTO_COMPACT`, `same batch`, `нативный компакт` — Простое удаление `DISABLE_AUTO_COMPACT` не гарантирует «compact at 95% then continue same batch»: официальный override может только понижать default threshold, а независимые Agent SDK repro фиксируют missed compact, iterator hang и overflow на tool batches · `https://code.claude.com/docs/en/env-vars`; `https://github.com/anthropics/claude-agent-sdk-typescript/issues/381`; `https://github.com/anthropics/claude-agent-sdk-python/issues/288`; `https://github.com/anthropics/claude-agent-sdk-python/issues/531` · 2026-09-03, #34
- `fact:post-response-cannot-rescue-admission` искать: `post-response`, `_finish_processing`, `zero query`, `после ответа` — Post-response-only compact не спасает reserve incident: current rejection завершается до `log_user/_ask_fn`, а `_finish_processing` не читает percentage и обрабатывает только уже установленный `compact_requested` · `chat_state.py:1020-1063,1117-1137`; `.orchestra/tasks/34/spikes/flow_probe.out` · 2026-09-03, #34
- `fact:night-timer-not-new-owner` искать: `55m`, `20%`, `night timer`, `ночной таймер` — Существующий Claude-only таймер 55m/20% нельзя сохранять неизменённым как независимого владельца новой 95%-политики: таймер компакти́рует idle Claude session при 20–94.9%, имеет отдельный SQLite episode claim и полностью выключен для native Codex · `chat_state.py:825-979`; `tests/test_preventive_compact.py`; `tests/test_compact_dispatch.py::test_preventive_timer_is_off_for_native_runtimes` · 2026-09-03, #34

## Пробелы

- `fact:crash-durable-batch-gap` искать: `crash durability`, `batch persistence`, `lost message`, `потеря сообщения` — Гарантия same-batch пока доказана только внутри живого процесса: до `log_user` current `chat_activity` хранит время/фазу, но не тело admitted batch; crash во время preflight compact не оставляет durable payload для автоматического продолжения · `message_log.py:42-55,70-124`; `.orchestra/tasks/34/research.md` § Counter-evidence · 2026-09-03, #34
- `fact:prompt-estimator-gap` искать: `prompt estimator`, `predicted current+prompt`, `95% semantics`, `оценка промпта` — Phase 2 должен заморозить runtime-normalized oracle для `(current context + fully assembled prompt) / window >= 95%`: Claude отдаёт fresh totals, а Codex current gauge основан на last input and upstream pre-turn compact omits incoming items · `claude_session.py:797-834`; `codex_session.py:930-995`; `https://github.com/openai/codex/blob/main/codex-rs/core/src/session/turn.rs` · 2026-09-03, #34
- `fact:provider-rejection-replay-gap` искать: `context_limit replay`, `side effects`, `duplicate prompt`, `повтор после лимита` — Автоматический replay после typed provider context rejection безопасен только при доказанных zero side effects и отсутствии/rollback persisted input; текущий normalized chunk contract такого доказательства не несёт · `response_stream.py:583-646`; `claude_session.py:564-605`; `codex_session.py:901-920`; `.orchestra/tasks/34/research.md` H6 · 2026-09-03, #34

## Источники

- `.orchestra/tasks/34/research.md` — Phase 1 evidence table, hypotheses, local probes, upstream sources, and the supported single-owner transaction.
- `.orchestra/tasks/14/` — origin of native-disable, 208K/80K reserve, night scheduler, and removal of the old 95% post-response owner.
- `.orchestra/tasks/16/` — Claude/Codex runtime split and native Codex compact lifecycle.
- `.orchestra/tasks/20/` — bounded Claude context-control recovery measurements.
- `.orchestra/tasks/24/` — verified Codex compact completion and stale-gauge recovery.
